<h2>Welcome to Get Rid Of It!</h2>
<h3> Listings: </h3>
<% @listings.each_with_index do |listing, index| %>
  <!--<%= "#{index+1}" %>.!--> <a href="/listings/<%=listing.id%>"><%= listing.title %></a> - $<%= listing.price %><br />
  <%= listing.location %><br />
  <%= listing.description %><br />
  <p>---</p>
<% end %>
