<h2> My Listings: </h2>

<% @listings.each_with_index do |listing, index| %>

  <!--<%= "#{index+1}" %>.!-->   <!--<%= "#{index+1}" %>.!--> <a href="/listings/<%=listing.id%>"><%= listing.title %></a> - $<%= listing.price %><br />
  <%= listing.location %><br />
  <%= listing.description %><br />
  <p>---</p>
<% end %>
